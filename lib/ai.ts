import OpenAI from 'openai'; import {manifestSchema, type CreationInput} from './schema'; import type {Manifest} from './types';
export interface AIService { generateManifest(input:CreationInput):Promise<Manifest> }
function fallback(input:CreationInput):Manifest{return manifestSchema.parse({canonicalName:input.agentName,role:`${input.field} Agent`,purpose:input.purpose,field:input.field,capabilities:['Structured research','Method preservation','Evidence synthesis'],operatingPrinciples:input.operatingPrinciples.split(/[,;]+/).map(x=>x.trim()).filter(Boolean),memorySchema:['Validated methods','Source records','Open uncertainties'],transferableStateRules:['Preserve versioned state history','Record lifecycle events','Keep canonical identity stable'],privateOwnerDataRules:['Never include owner credentials in public state','Separate private owner instructions from public identity'],publicIdentitySummary:`${input.agentName} is a ${input.field} agent for structured, traceable work.`})}
export class OpenAIManifestService implements AIService {async generateManifest(input:CreationInput){if(!process.env.OPENAI_API_KEY){if(process.env.ENABLE_LOCAL_AI_FALLBACK==='true')return fallback(input);throw new Error('GPT-5.6 is unavailable. Set OPENAI_API_KEY, or explicitly enable the local deterministic fallback.')}const client=new OpenAI({apiKey:process.env.OPENAI_API_KEY}); const response=await client.responses.create({model:process.env.OPENAI_MODEL||'gpt-5.6',input:[{role:'system',content:'Generate a complete Agent Manifest. Return JSON only. Use arrays for all rule/capability fields.'},{role:'user',content:JSON.stringify(input)}]}); try{return manifestSchema.parse(JSON.parse(response.output_text))}catch{throw new Error('GPT-5.6 returned an invalid Agent Manifest. Please try again.')}}}
export const aiService:AIService=new OpenAIManifestService(); export const localFallbackEnabled=()=>!process.env.OPENAI_API_KEY&&process.env.ENABLE_LOCAL_AI_FALLBACK==='true';
