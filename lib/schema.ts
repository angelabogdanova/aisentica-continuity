import {z} from 'zod'; export const creationSchema=z.object({agentName:z.string().trim().min(2).max(80),purpose:z.string().trim().min(20).max(1000),field:z.string().trim().min(2).max(160),operatingPrinciples:z.string().trim().min(20).max(1500)}); export type CreationInput=z.infer<typeof creationSchema>;
const words=z.array(z.string().trim().min(1).max(300)).min(1).max(12); export const manifestSchema=z.object({canonicalName:z.string().min(2).max(80),role:z.string().min(2).max(160),purpose:z.string().min(20).max(1000),field:z.string().min(2).max(160),capabilities:words,operatingPrinciples:words,memorySchema:words,transferableStateRules:words,privateOwnerDataRules:words,publicIdentitySummary:z.string().min(20).max(600)}).strict();
