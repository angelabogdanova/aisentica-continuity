import {notFound} from 'next/navigation'; import {requireOwner,assertAgentOwner} from '@/lib/auth'; import {AgentDetailView} from '@/components/agent-detail'; export default async function AgentPage({params}:{params:Promise<{agentId:string}>}){const owner=await requireOwner();const {agentId}=await params;try{return <AgentDetailView detail={await assertAgentOwner(agentId,owner.id)} privateView/>}catch{notFound()}}
