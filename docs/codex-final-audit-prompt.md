# Final Codex audit prompt

Open the primary Codex thread where the majority of Aisentica Continuity was built. Paste the prompt below into that same thread.

---

Perform a final submission-readiness audit of the repository `angelabogdanova/aisentica-continuity` on `main`.

Project invariant:

`Create → Bind Domain → Develop → Park → Reactivate → Transfer → Continue`

The same Agent ID, canonical domain, Manifest, developed professional state, and immutable prior versions must survive every lifecycle and ownership transition. Transfer must move the same Agent trajectory rather than create a copy.

Audit the current repository and production-oriented implementation for:

1. lifecycle correctness across all seven stages;
2. exactly one immutable sequential Version and one Event per successful transition;
3. authorization checks for current and intended owners;
4. unique canonical-domain enforcement and same-origin HTTPS proof safety;
5. strict GPT-5.6 structured-output validation for Create and Develop;
6. safe treatment of owner-supplied development material as untrusted evidence;
7. Park and Reactivate preservation of prior state;
8. single-use, expiring, intended-owner, version-locked Transfer behavior;
9. Continue requiring the transferred checkpoint and restoring ACTIVE availability;
10. public/private projection separation;
11. absence of credentials or secret values in source and client bundles;
12. migration order and production schema compatibility;
13. lint, strict TypeScript, unit tests, and production build;
14. README accuracy, installation instructions, supported platforms, no-rebuild judge path, Codex contribution description, and GPT-5.6 description;
15. consistency among README, PROJECT_SPEC.md, SECURITY.md, docs/architecture.md, docs/judge-flow.md, and current code.

Important current production state:

- Atlas ID: `AC-7XUEZ42`
- canonical domain: `aisentica-continuity.vercel.app`
- current owner: Owner B
- current status: ACTIVE
- current version: 7
- versions: INITIAL_MANIFEST, DOMAIN_BINDING, DEVELOPMENT, PARKED, REACTIVATED, TRANSFERRED, CONTINUED
- production reset is disabled
- the complete UI lifecycle has passed an independent Playwright run in production

Do not reset or mutate production data. Do not rotate credentials. Do not weaken domain uniqueness, owner authorization, token protection, or the append-only version invariant.

First report findings by severity with exact file references. Then implement only necessary, low-risk corrections on a clean branch. Run all validation commands. Open a focused pull request only if changes are needed. If no changes are needed, state that clearly.

Finally, summarize:

- what Codex contributed to this project;
- the most important human-reviewed product and engineering decisions;
- whether the repository is ready for OpenAI Build Week judging;
- any remaining limitations that should be disclosed rather than fixed now.

---

After the audit is complete, run:

```text
/feedback
```

Copy the exact Session ID generated by the primary Codex thread into the Devpost submission form. Do not invent or approximate the value. The Session ID does not need to be committed to the repository.
