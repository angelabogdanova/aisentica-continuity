const steps=['Create','Bind Domain','Develop','Park','Transfer','Continue']; export function Lifecycle(){return <ol className="grid grid-cols-2 gap-2 sm:grid-cols-6">{steps.map((s,i)=><li key={s} className={`border p-3 text-sm ${i===0?'border-red-500 bg-red-500/10 text-white':'border-zinc-800 text-zinc-500'}`}><b className="block">{String(i+1).padStart(2,'0')} {s}</b><span className="text-xs">{i===0?'Available':'Next phase'}</span></li>)}</ol>}
