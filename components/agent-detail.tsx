import Link from 'next/link'; import type {AgentDetail} from '@/lib/types'; export function AgentDetailView({detail,privateView}:{detail:AgentDetail;privateView:boolean}){const {agent,owner,version,events}=detail;return <main className="mx-auto max-w-5xl px-5 py-12"><div className="flex flex-wrap justify-between gap-4"><div><p className="eyebrow">{agent.id} · State version {agent.currentVersion}</p><h1 className="mt-2 text-4xl font-bold">{agent.canonicalName}</h1><p className="mt-2 text-zinc-400">{agent.role}</p></div>{privateView&&<Link className="btn-muted" href={`/public/agents/${agent.id}`}>View public card</Link>}</div><dl className="card mt-8 grid gap-4 sm:grid-cols-3"><div><dt className="text-xs text-zinc-500">Status</dt><dd>Active</dd></div><div><dt className="text-xs text-zinc-500">Owner</dt><dd>{privateView?owner.displayName:'Not disclosed'}</dd></div><div><dt className="text-xs text-zinc-500">Created</dt><dd>{new Date(agent.createdAt).toLocaleDateString()}</dd></div><div className="sm:col-span-3"><dt className="text-xs text-zinc-500">Purpose</dt><dd>{agent.purpose}</dd></div></dl><section className="card mt-5"><h2 className="text-xl font-bold">{privateView?'Structured Agent Manifest':'Public identity'}</h2>{privateView?<pre className="mt-4 overflow-auto whitespace-pre-wrap text-sm text-zinc-300">{JSON.stringify(version.stateJson,null,2)}</pre>:<p className="mt-3 text-zinc-300">{version.stateJson.publicIdentitySummary}</p>}</section>{privateView&&<section className="card mt-5"><h2 className="text-xl font-bold">State timeline</h2>{events.map(e=><div key={e.id} className="mt-4 border-l-2 border-red-500 pl-4"><b>v1 · CREATE</b><p className="text-sm text-zinc-400">{version.changeSummary} {new Date(e.createdAt).toLocaleString()}</p></div>)}</section>}</main>}
